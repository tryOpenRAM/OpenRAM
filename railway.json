{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "(npm ci --prefix web || npm install --prefix web) && npm run build --prefix web && (npm ci --prefix arena || npm install --prefix arena)"
  },
  "deploy": {
    "startCommand": "npm start --prefix arena",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
